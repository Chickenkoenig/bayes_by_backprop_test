# @package _global_
defaults:
  - override /data: large_sin_gap
  - override /model: ensemble
  - override /model/architecture: bbb
  - override /model/ensemble_type: bbb_ensemble
  - override /train: base_train

exp_name: large_sin_gap_bbb_ensemble



hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model.architecture.activation: 'ReLU'
      model.architecture.kl_weight: 0.01
      train.epochs: 8000
      train.learning_rate: 0.01
defaults:
  - override /data: large_sin_gap
  - override /model: ensemble
  - override /model/ensemble_type: ensembles/swag_ensembles
  - override /train: base_train

name: large_sin_gap_swag_ensemble

hydra:
  sweeper:
    params:
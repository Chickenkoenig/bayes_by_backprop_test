# @package _global_
defaults:
  - override /data: large_sin_gap
  - override /model: single_model
  - override /model/architecture: architecture/swag
  - override /train: base_train

name: large_sin_gap_swag_single

hydra:
  sweeper:
    params:

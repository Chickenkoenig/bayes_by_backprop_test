# @package _global_
defaults:
  - override /data: large_sin_gap
  - override /model: ensemble
  - override /model/ensemble_type: ensembles/mlp_ensembles
  - override /train: base_train

name: large_sin_gap_mlp_ensemble

hydra:
  sweeper:
    params:
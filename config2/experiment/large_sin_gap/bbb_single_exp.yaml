defaults:
  - override /data: large_sin_gap
  - override /model: single_model
  - override /model/architecture: architecture/bbb
  - override /train: base_train

name: large_sin_gap_bbb_single

hydra:
  sweeper:
    params: